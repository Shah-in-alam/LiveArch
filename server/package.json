{
  "name": "livearch-server",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted LiveArch — permanent shareable diagram URLs, live sync, and accounts (Neon Postgres or filesystem).",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
