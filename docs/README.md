# docs/

Place for documentation assets.

## demo.gif (TODO)

The main README references `docs/demo.gif`. To record it:

1. Run `livearch` on a sample project.
2. Open `http://localhost:7842` in a browser next to your editor.
3. Save a few files and capture the live diagram updating (new nodes flash, toast appears).
4. Export a short GIF (≤ 5s) and save it here as `demo.gif`.
